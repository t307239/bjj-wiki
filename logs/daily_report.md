## 2026-04-01 分析レポート（本日 cron 実行後・確定版）

> 生成時刻: 2026-04-01 自動タスク（Cowork Scheduled Task）

### 実行結果サマリ

| 指標 | 値 |
|---|---|
| 実行日時 | 2026-04-01 JST（cron 9:10 JST 実行） |
| 処理件数 | **80 件** |
| ✅ 更新成功 | **80 件**（100%） |
| ⏭️ スキップ（設定済み） | 0 件 |
| ⏳ キュー追加（明日処理） | 0 件 |
| ⚠️ 動画なし（マッチなし） | 0 件 |
| ❌ DB更新失敗 / 検索エラー | 0 件 |
| 📊 本日の API 呼び出し数 | **80 / 80（全消化）** |
| キュー残件数（翌日分） | **45 件** |

> 🎉 **完璧なラン！** 80件全件が動画 URL 更新済み。エラー・マッチなしゼロ。

**処理範囲**: `athlete-andre-galvao` → `bjj-butterfly-hook-game`
**バックエンド**: LocalYouTubeSearcher (requests + ytInitialData)

---

### 累積進捗

| 指標 | 値 |
|---|---|
| 全 EN ページ数（推定） | **1,566 ページ** |
| キュー残件数（翌日分） | **45 件**（2026-03-30 キュー未消化） |
| 本日処理ページ数 | **80 件** |
| 3日間合計（3/30〜4/1） | **240 件** 更新済み |

> ⚠️ **キュー未処理の注意**: 2026-03-30 にキューに積まれた45件（`bjj-arm-in-guillotine` 〜 `bjj-back-escape-roll-guide` 等）が引き続き未処理。
> crontab コマンドが `--limit 80`（キュー処理フラグなし）のため、明示的に `--from-queue` を使わない限り消化されない。
> → 手動で `python3 scripts/local_video_fetcher.py --from-queue` を実行するか、crontab に `--from-queue` オプションを追加推奨。

---

### エラーパターン分析

**検出されたエラー: なし**

- `ytInitialData が見つかりません`: 0件 ✅
- `JSON パース失敗`: 0件 ✅
- `検索エラー` / タイムアウト: 0件 ✅
- `日次上限到達` でのキュー追加: 0件 ✅
- `適切な動画が見つかりませんでした`: 0件 ✅

> ℹ️ `urllib3 v2 + LibreSSL 2.8.3` の `NotOpenSSLWarning` が継続出力中だが、動作に影響なし（既知の環境依存警告）。

---

### スクリプト品質サマリ

スコアリングは全体的に良好：

- 最高スコア例: `bjj-back-attacks-guide` (+95pt)、`bjj-arm-drag` (+85pt)、`bjj-butterfly-guard` (+75pt)
- 最低スコア例: `bjj-bjj-culture-guide` (+40pt)、`bjj-bjj-for-women` (+40pt) ← 一般的な動画でマッチングがやや弱い
- 平均スコア（目視）: **約 60pt 前後** — 品質維持 ✅

---

### 改善提案

#### 要手動確認: キュー消化フロー

**現象**: crontab が `--from-queue` なしで実行されるため、キューが積みあがっても自動消化されない。

**現在のキュー**: 45件（`bjj-arm-in-guillotine` 系など）

**推奨対応**:
```bash
# 手動でキュー消化（45件 < 80件上限なので1回で完了）
python3 ~/Claude/bjj-wiki/scripts/local_video_fetcher.py --from-queue
```

または crontab を2行化:
```
10 9 * * *  python3 ~/Claude/bjj-wiki/scripts/local_video_fetcher.py --from-queue --limit 80
20 9 * * *  python3 ~/Claude/bjj-wiki/scripts/local_video_fetcher.py --limit 80
```

これにより「キュー消化 → 通常バッチ」の順で毎日実行され、積み残しゼロになる。

---

## 2026-03-31 分析レポート（本日 cron 実行後・確定版）

> 生成時刻: 2026-03-31 自動タスク（Cowork Scheduled Task）

### 実行結果サマリ

| 指標 | 値 |
|---|---|
| 実行日時 | 2026-03-31 JST（cron 9:10 JST 実行） |
| 処理件数 | **80 件** |
| ✅ 更新成功 | **80 件**（100%） |
| ⏭️ スキップ（設定済み） | 0 件 |
| ⏳ キュー追加（明日処理） | 0 件 |
| ⚠️ 動画なし（マッチなし） | 0 件 |
| ❌ DB更新失敗 / 検索エラー | 0 件 |
| 📊 本日の API 呼び出し数 | **80 / 80（全消化）** |
| キュー残件数（翌日分） | **45 件** |

> 🎉 **完璧なラン！** 80件全件が動画 URL 更新済み。エラー・マッチなしゼロ。

**処理範囲**: `bjj-arm-in-guillotine` → `bjj-beginner-mistakes-avoid`
**バックエンド**: LocalYouTubeSearcher (requests + ytInitialData)

---

### 累積進捗

| 指標 | 値 |
|---|---|
| 全 EN ページ数 | **1,566 ページ** |
| キュー残件数（翌日分） | **45 件** |
| 本日処理ページ数 | **80 件** |
| 翌日完了見込み | 残り45件 < 80件上限 → **明日1回で全消化** |

> 🏁 **明日（2026-04-01）の実行でキューが全完了し、Content 99 → 100 達成見込み**

---

### エラーパターン分析

**検出されたエラー: なし**

- `ytInitialData が見つかりません`: 0件 ✅
- `JSON パース失敗`: 0件 ✅
- `検索エラー` / タイムアウト: 0件 ✅
- `日次上限到達` でのキュー追加: 0件 ✅
- `適切な動画が見つかりませんでした`: 0件 ✅

> ℹ️ `urllib3 v2 + LibreSSL 2.8.3` の `NotOpenSSLWarning` が継続出力中だが、動作に影響なし（既知の環境依存警告）。
